package com.example.malakfinal.network;

import kotlin.coroutines.Continuation;

public class GenerativeModel {
    public GenerativeModel(String geminiVersion, String geminiApiKey) {
    }

    public void generateContent(String prompt, Continuation<GenerateContentResponse> continuation) {
    }

    public void generateContent(String prompt, Continuation<GenerateContentResponse> continuation) {
    }
}
